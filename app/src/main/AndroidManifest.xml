<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.playstore.sqlite1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/doctorapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sqlite1">
        <activity android:name=".just_for_test"></activity>
        <activity android:name=".consultation_information" />
        <activity android:name=".consultation_list" />
        <activity android:name=".activity_user_profile" />
        <activity android:name=".medicam_information" />
        <activity android:name=".patient_informations" />
        <activity android:name=".Calculate" />
        <activity android:name=".SelectMedPat" />
        <activity android:name=".update_medicam" />
        <activity android:name=".medicament_list" />
        <activity
            android:name=".FirstPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".addMedical" />
        <activity android:name=".add_patient" />
        <activity android:name=".update_contact" />
        <activity android:name=".patientList" />
        <activity
            android:name=".login"
            android:theme="@style/Theme.Design.Light.NoActionBar" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>